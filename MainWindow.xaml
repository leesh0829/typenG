<Window x:Class="typenG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="typenG"
        Topmost="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Width="980"
        Height="180"
        ShowInTaskbar="True"
        SizeChanged="Window_OnSizeChanged"
        KeyDown="Window_KeyDown"
        PreviewTextInput="Window_PreviewTextInput"
        PreviewMouseLeftButtonDown="Window_PreviewMouseLeftButtonDown"
        PreviewMouseRightButtonUp="Window_PreviewMouseRightButtonUp">
    <Window.ContextMenu>
        <ContextMenu x:Name="OverlayContextMenu">
            <MenuItem x:Name="ResizeMenuItem" Header="위치/크기 조정 시작" Click="ResizeMenuItem_OnClick"/>
            <MenuItem Header="다른 문장 하기" Click="NextPassageMenuItem_OnClick"/>
            <MenuItem Header="연습 언어">
                <MenuItem x:Name="MixedLanguageMenuItem" Header="한/영 섞기" Click="MixedLanguageMenuItem_OnClick" IsCheckable="True" IsChecked="True"/>
                <MenuItem x:Name="KoreanLanguageMenuItem" Header="한글만" Click="KoreanLanguageMenuItem_OnClick" IsCheckable="True"/>
                <MenuItem x:Name="EnglishLanguageMenuItem" Header="영어만" Click="EnglishLanguageMenuItem_OnClick" IsCheckable="True"/>
            </MenuItem>
            <Separator/>
            <MenuItem x:Name="ForegroundMenuItem" Header="글자 색상: 검정" Click="ForegroundMenuItem_OnClick"/>
            <Separator/>
            <MenuItem Header="프로그램 종료" Click="ExitMenuItem_OnClick"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Grid Margin="8" SnapsToDevicePixels="True">
        <Border x:Name="MainFrameBorder"
                CornerRadius="10"
                Background="#01000000"
                Padding="8"
                BorderBrush="Transparent"
                BorderThickness="0">
            <Grid ClipToBounds="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="ProgressText"
                           Grid.Row="0"
                           Grid.RowSpan="3"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Top"
                           Margin="0,0,0,0"
                           FontFamily="Consolas"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Opacity="0.75"
                           Visibility="Collapsed"/>

                <TextBlock x:Name="CurrentLineText"
                           Grid.Row="1"
                           FontFamily="Consolas"
                           FontSize="42"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           TextWrapping="NoWrap"
                           RenderTransformOrigin="0.5,0.5">
                    <TextBlock.RenderTransform>
                        <TranslateTransform x:Name="CurrentTransform"/>
                    </TextBlock.RenderTransform>
                </TextBlock>

                <TextBlock x:Name="NextLineText"
                           Grid.Row="1"
                           FontFamily="Consolas"
                           FontSize="42"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Opacity="0"
                           TextWrapping="NoWrap"
                           RenderTransformOrigin="0.5,0.5">
                    <TextBlock.RenderTransform>
                        <TranslateTransform x:Name="NextTransform" Y="90"/>
                    </TextBlock.RenderTransform>
                </TextBlock>

                <TextBlock x:Name="UpcomingLineText"
                           Grid.Row="2"
                           FontFamily="Consolas"
                           FontSize="24"
                           FontWeight="SemiBold"
                           VerticalAlignment="Bottom"
                           HorizontalAlignment="Center"
                           Margin="0,2,0,0"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis"
                           Opacity="0.55"
                           Visibility="Collapsed"/>
            
                <Thumb x:Name="TopLeftResizeThumb"
                       Grid.RowSpan="3"
                       Width="18"
                       Height="18"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Margin="0"
                       Cursor="SizeNWSE"
                       DragDelta="ResizeThumb_OnDragDelta"
                       Visibility="Collapsed">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="#AA111111" CornerRadius="9" BorderBrush="White" BorderThickness="1"/>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

                <Thumb x:Name="TopResizeThumb"
                       Grid.RowSpan="3"
                       Height="8"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Top"
                       Margin="14,0"
                       Cursor="SizeNS"
                       DragDelta="ResizeThumb_OnDragDelta"
                       Visibility="Collapsed">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="#01000000"/>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

                <Thumb x:Name="TopRightResizeThumb"
                       Grid.RowSpan="3"
                       Width="18"
                       Height="18"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Top"
                       Margin="0"
                       Cursor="SizeNESW"
                       DragDelta="ResizeThumb_OnDragDelta"
                       Visibility="Collapsed">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="#AA111111" CornerRadius="9" BorderBrush="White" BorderThickness="1"/>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

                <Thumb x:Name="LeftResizeThumb"
                       Grid.RowSpan="3"
                       Width="8"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Stretch"
                       Margin="0,14"
                       Cursor="SizeWE"
                       DragDelta="ResizeThumb_OnDragDelta"
                       Visibility="Collapsed">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="#01000000"/>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

                <Thumb x:Name="RightResizeThumb"
                       Grid.RowSpan="3"
                       Width="8"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Stretch"
                       Margin="0,14"
                       Cursor="SizeWE"
                       DragDelta="ResizeThumb_OnDragDelta"
                       Visibility="Collapsed">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="#01000000"/>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

                <Thumb x:Name="BottomLeftResizeThumb"
                       Grid.RowSpan="3"
                       Width="18"
                       Height="18"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Bottom"
                       Margin="0"
                       Cursor="SizeNESW"
                       DragDelta="ResizeThumb_OnDragDelta"
                       Visibility="Collapsed">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="#AA111111" CornerRadius="9" BorderBrush="White" BorderThickness="1"/>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

                <Thumb x:Name="BottomResizeThumb"
                       Grid.RowSpan="3"
                       Height="8"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Bottom"
                       Margin="14,0"
                       Cursor="SizeNS"
                       DragDelta="ResizeThumb_OnDragDelta"
                       Visibility="Collapsed">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="#01000000"/>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

                <Thumb x:Name="BottomRightResizeThumb"
                       Grid.RowSpan="3"
                       Width="18"
                       Height="18"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Bottom"
                       Margin="0"
                       Cursor="SizeNWSE"
                       DragDelta="ResizeThumb_OnDragDelta"
                       Visibility="Collapsed">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Border Background="#AA111111" CornerRadius="9" BorderBrush="White" BorderThickness="1"/>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>
            </Grid>
        </Border>
    </Grid>
</Window>
